name: shared_models
description: Pure data models shared across the student ecosystem apps.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.4.0
  intl: ^0.19.0

flutter:
  uses-material-design: true
