name: staff_app
description: Faculty Workspace — Staff Portal for classroom tools and student management.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  fl_chart: ^0.68.0
  provider: ^6.1.2
  file_picker: ^8.0.0
  csv: ^6.0.0
  uuid: ^4.4.0
  intl: ^0.19.0
  shared_models:
    path: ../../packages/shared_models

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
